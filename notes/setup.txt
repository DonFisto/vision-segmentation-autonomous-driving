
conda create -n seg python=3.10 -y
conda activate seg

pip install -U pip setuptools wheel
pip install numpy scipy pillow matplotlib seaborn
pip install opencv-python

pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu126

pip install -U openmim
mim install mmengine
mim install "mmcv>=2.0.0"
pip install mmsegmentation
pip install tqdm pyyalm albumentations
pip install scikit-image scikit-learn
pip install tensorboard

mkdir -p vision-seg/{configs,datasets,runs,scripts,notes,test_images,out}
cd vision-seg/
git init

wget -O test_images/street.jpg https://raw.githubusercontent.com/open-mmlab/mmsegmentation/main/demo/demo.png

